#include "donjongenerator.h"

#define random(min, max) (rand()%(max))+min;

DonjonGenerator::DonjonGenerator(unsigned nbSalle, unsigned hauteur, unsigned largeur)
{
//    this->hauteur=hauteur;
//    this->largeur=largeur;
//    this->nbSalle=nbSalle;

//    for(unsigned i=0;i<hauteur;i++)
//        for(unsigned j=0;j<largeur;j++)
//            ppbSalle[i][j]=nullptr;
}

DonjonGenerator::~DonjonGenerator()
{
//    for(unsigned i=0;i<hauteur;i++)
//        for(unsigned j=0;j<largeur;j++)
//            if(ppbSalle[i][j]!=nullptr)
//                delete(ppbSalle[i][j]);
}

void DonjonGenerator::ajouterSalle()
{
//    salle *tmpSalle;
//    // Si il n'y a pas encore de salle on en place une au pif
//    if(salleCree.size()==) {
//        tmpSalle = new salle(random(0, hauteur), random(0, largeur));
//        ppbSalle[tmpSalle->h][tmpSalle->l]=tmpSalle;
//        salleCree.append(tmpSalle);
//    }
}
